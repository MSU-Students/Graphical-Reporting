import { MutationTree } from 'vuex';
import { TableStateInterface } from './state';

const mutation: MutationTree<TableStateInterface> = {
  someMutation (/* state: ExampleStateInterface */) {
    // your code
  }
};

export default mutation;
